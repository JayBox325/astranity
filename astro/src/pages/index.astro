---
import Layout from '../layouts/Layout.astro'
import { useSanityClient } from 'astro-sanity'

const homepage = await useSanityClient().fetch(`*[_type == "homepage"][0]{
	mainImage,
	pageBuilder,
	fieldSeo
}`)
console.log('homepage', homepage)
---

<Layout title="Welcome to Astro.">
	<h1>Homepage</h1>
</Layout>