---
import Accordions from '../Accordions/Accordions.astro';
import BodyText from '../BodyText/index.astro'
const { content } = Astro.props || {}
---

{content?.length ? (
    <>
        {content.map((item,n) => {
            switch (item._type) {
                case 'accordions':
                    return <Accordions content={item} />
                    break;
                case 'bodyText':
                    return <BodyText content={item} />
                    break;
            
                default:
                    return null
                    break;
            }
        })}
    </>
) : ''}